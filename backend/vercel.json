{
  "version": 2,
  "installCommand": "cd .. && pnpm install",
  "buildCommand": "cd .. && pnpm run build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ],
  "functions": {
    "api/index.ts": {
      "maxDuration": 60
    }
  }
}