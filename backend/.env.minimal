# ============================================================================
# G-Rump Minimal Configuration - Get Started in 60 Seconds
# ============================================================================
#
# OPTION 1: Try G-Rump immediately with MOCK MODE (no API key needed)
# Perfect for: exploring features, testing, development
# ----------------------------------------------------------------------------
MOCK_AI_MODE=true
#
# OPTION 2: Use real AI with NVIDIA NIM (Powered by NVIDIA)
# Perfect for: production use, running with world-class models
# ----------------------------------------------------------------------------
# Get your free API key at https://build.nvidia.com/
# NVIDIA_NIM_API_KEY=nvapi-your-key-here
#
# Featured models:
# - meta/llama-3.1-405b-instruct (flagship, complex tasks)
# - meta/llama-3.1-70b-instruct (balanced, default)
# - mistralai/codestral-22b-instruct-v0.1 (code generation)
# - nvidia/llama-3.1-nemotron-ultra-253b-v1 (reasoning)
#
# ============================================================================
# Basic server configuration (defaults are fine for development)
# ============================================================================

NODE_ENV=development
PORT=3000

# ============================================================================
# That's it! Everything else is optional.
#
# Once you've chosen an option above, you can start the app immediately:
#   npm run dev
#
# For production deployment or to enable optional features like RAG,
# voice, integrations, or webhooks, see backend/.env.example for the full
# list of available configuration options.
# ============================================================================
