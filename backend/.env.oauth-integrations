# ============================================================================
# OAuth Integration Provider Credentials
# ============================================================================
# These credentials enable users to connect their third-party service accounts
# to G-Rump for seamless integrations.
#
# SETUP INSTRUCTIONS:
# 1. Visit each provider's developer portal to create an OAuth application
# 2. Set the callback URL to: {YOUR_BASE_URL}/api/integrations-v2/oauth/{provider}/callback
# 3. Copy the Client ID and Client Secret to the variables below
# 4. Add these variables to your main .env file
# ============================================================================

# Public base URL (required for OAuth callbacks)
PUBLIC_BASE_URL=http://localhost:3000

# ── Slack ────────────────────────────────────────────────────────────────────
# Create app at: https://api.slack.com/apps
# Callback URL: {PUBLIC_BASE_URL}/api/integrations-v2/oauth/slack/callback
# Scopes: chat:write, channels:read, users:read
SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=

# ── GitHub ───────────────────────────────────────────────────────────────────
# Create app at: https://github.com/settings/developers
# Callback URL: {PUBLIC_BASE_URL}/api/integrations-v2/oauth/github/callback
# Scopes: repo, read:user, user:email
GITHUB_OAUTH_CLIENT_ID=
GITHUB_OAUTH_CLIENT_SECRET=

# ── Notion ───────────────────────────────────────────────────────────────────
# Create integration at: https://www.notion.so/my-integrations
# Callback URL: {PUBLIC_BASE_URL}/api/integrations-v2/oauth/notion/callback
# Capabilities: Read content, Update content, Insert content
NOTION_CLIENT_ID=
NOTION_CLIENT_SECRET=

# ── Linear ───────────────────────────────────────────────────────────────────
# Create app at: https://linear.app/settings/api
# Callback URL: {PUBLIC_BASE_URL}/api/integrations-v2/oauth/linear/callback
# Scopes: read, write
LINEAR_CLIENT_ID=
LINEAR_CLIENT_SECRET=

# ── Figma ────────────────────────────────────────────────────────────────────
# Create app at: https://www.figma.com/developers/apps
# Callback URL: {PUBLIC_BASE_URL}/api/integrations-v2/oauth/figma/callback
# Scopes: file_read
FIGMA_CLIENT_ID=
FIGMA_CLIENT_SECRET=

# ── Spotify ──────────────────────────────────────────────────────────────────
# Create app at: https://developer.spotify.com/dashboard/applications
# Callback URL: {PUBLIC_BASE_URL}/api/integrations-v2/oauth/spotify/callback
# Scopes: user-read-playback-state, user-modify-playback-state, playlist-read-private
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=

# ── Google (Gmail & Calendar) ────────────────────────────────────────────────
# Create project at: https://console.cloud.google.com/
# Enable APIs: Gmail API, Google Calendar API
# Create OAuth credentials
# Callback URLs:
#   - {PUBLIC_BASE_URL}/api/integrations-v2/oauth/gmail/callback
#   - {PUBLIC_BASE_URL}/api/integrations-v2/oauth/google_calendar/callback
# Scopes:
#   Gmail: https://www.googleapis.com/auth/gmail.send, https://www.googleapis.com/auth/gmail.readonly
#   Calendar: https://www.googleapis.com/auth/calendar.readonly, https://www.googleapis.com/auth/calendar.events
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=

# ── Twitter/X ────────────────────────────────────────────────────────────────
# Create app at: https://developer.twitter.com/en/portal/dashboard
# Callback URL: {PUBLIC_BASE_URL}/api/integrations-v2/oauth/twitter/callback
# Scopes: tweet.read, tweet.write, users.read
TWITTER_CLIENT_ID=
TWITTER_CLIENT_SECRET=

# ── GitLab ───────────────────────────────────────────────────────────────────
# Create app at: https://gitlab.com/-/profile/applications
# Callback URL: {PUBLIC_BASE_URL}/api/integrations-v2/oauth/gitlab/callback
# Scopes: api, read_user, read_repository
GITLAB_CLIENT_ID=
GITLAB_CLIENT_SECRET=

# ── Bitbucket ────────────────────────────────────────────────────────────────
# Create app at: https://bitbucket.org/account/settings/app-passwords/
# Callback URL: {PUBLIC_BASE_URL}/api/integrations-v2/oauth/bitbucket/callback
# Scopes: repository, pullrequest
BITBUCKET_CLIENT_ID=
BITBUCKET_CLIENT_SECRET=

# ── Jira (Atlassian) ─────────────────────────────────────────────────────────
# Create app at: https://developer.atlassian.com/console/myapps/
# Callback URL: {PUBLIC_BASE_URL}/api/integrations-v2/oauth/jira/callback
# Scopes: read:jira-work, write:jira-work
JIRA_CLIENT_ID=
JIRA_CLIENT_SECRET=

# ============================================================================
# SETUP CHECKLIST
# ============================================================================
# [ ] Set PUBLIC_BASE_URL to your production domain (e.g., https://api.g-rump.com)
# [ ] Create OAuth apps for each provider you want to support
# [ ] Configure callback URLs in each provider's settings
# [ ] Copy Client IDs and Secrets to this file
# [ ] Test OAuth flow in development before deploying to production
# [ ] Ensure database has oauth_tokens table for storing tokens securely
# ============================================================================
