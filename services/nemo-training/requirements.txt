# NeMo Framework fine-tuning for G-Rump
# Requires GPU (run on NGC VM or NGC container)
# Python 3.10+ required

# NeMo AutoModel for SFT / LoRA fine-tuning
# See: https://docs.nvidia.com/nemo-framework/user-guide/latest/automodel/sft.html
nemo-automodel>=25.2.0
nemo-run>=25.2.0

# Or install from NGC container:
# docker run --gpus all -it nvcr.io/nvidia/nemo:25.02.framework
